@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Content available: @Model.imgs.Count</a>.</p>
    <body>

        <table border="1" >

            <tr>
                <th></th>
                <th></th>
                <th></th>
                <th></th>
            </tr>

            @foreach (var image in Model.chunks)
            {<tr>
                <td>
                    <img src=@image[0] height="400" />
                    <img src=@image[1] height="400" />
                    <img src=@image[2] height="400" />
                    <img src=@image[3] height="400" />
                </td>
             </tr>
            }

        </table>
    </body>
</div>
